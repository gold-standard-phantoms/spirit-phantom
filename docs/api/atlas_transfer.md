# Atlas transfer

Affine nearest-neighbour transfer of atlas labels into a target image's 3-D
spatial grid. Used by both `analyse map-mask` and `analyse eg-mask`.

::: spirit_phantom.io.atlas_transfer
